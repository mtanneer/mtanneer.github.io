---
interface Props {
  label: string;
}
const { label } = Astro.props;
---

<span class="font-mono text-[10px] rounded-sm bg-[var(--accent-dim)] px-2 py-0.5 tracking-wide text-[var(--accent-text)]">{label}</span>
