# mtanneer.github.io

Docs index (coming soon) — will aggregate auto-generated documentation from other repos I own.

The portfolio previously hosted here has moved to [manastanneeru.com](https://manastanneeru.com), deployed from [mtanneer/manastanneeru-com](https://github.com/mtanneer/manastanneeru-com) on Cloudflare Pages.

Deployed via GitHub Pages using a GitHub Actions workflow (`.github/workflows/deploy.yml`) that uploads the repo root as-is — no build step, `index.html` served directly.
